#!/usr/bin/env python

from setuptools import setup


setup(name='windirs',
      version='1.00',
      description='Windows path wrapper',
      author='Isaac Robinson',
      author_email='isaac@rirobinson.com',
      entry_points=dict(windirs=['windirs = windirs']),
      url=''
      )
